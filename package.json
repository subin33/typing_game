{
  "name": "typing_game",
  "version": "1.0.0",
  "description": "typing game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "_comment": "npm install 시 -D 를 붙이면 devDependencies 로 들어가는데 devDependencies는 개발용 dependencies 실제 사용용이다. ",
  "dependencies": {
    "axios": "^1.7.7",
    "toastify-js": "^1.12.0"
  }
}
